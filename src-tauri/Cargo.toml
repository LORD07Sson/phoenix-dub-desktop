[package]
name = "phoenix-dub-desktop"
version = "0.4.2"
description = "PHOENIX DUB Desktop — клиент студии дубляжа"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-single-instance = "2"
tauri-plugin-shell = "2"
tauri-plugin-dialog = "2"
tauri-plugin-notification = "2"
tauri-plugin-autostart = "2"
tauri-plugin-global-shortcut = "2"
tauri-plugin-store = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# Автообновление — Velopack вместо tauri-plugin-updater+NSIS-инсталлятора:
# GithubSource читает релизы репозитория напрямую (он публичный, прокси
# на своём сервере не нужен), тихая установка без UAC, задел на дельта-
# патчи вместо полного скачивания каждый раз. См. main.rs.
velopack = "1.2.0"
keyring = { version = "3", features = ["apple-native", "windows-native", "sync-secret-service"] }
# Загрузка файла, перетащенного из проводника (upload_report_file в
# main.rs) — multipart POST на свой API напрямую из Rust, минуя
# JS/fetch: тогда не нужен tauri-plugin-fs с его capability-скоупом на
# ЛЮБОЙ путь на диске (drag&drop роняет путь произвольного файла
# пользователя, не только из appdata/temp) — команда сама читает файл
# std::fs::read по пришедшему пути и сама шлёт его на сервер.
# rustls вместо native-tls — не тащит системный OpenSSL на сборку.
reqwest = { version = "0.12", default-features = false, features = ["multipart", "json", "rustls-tls"] }

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true

[[bin]]
name = "phoenix-dub-desktop"
path = "src/main.rs"
