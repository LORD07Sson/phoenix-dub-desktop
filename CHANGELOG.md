# Changelog

Формат — по мотивам [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/).
Версии — `VERSION`/`src-tauri/tauri.conf.json`, дата — день слияния в `main`.

## [Unreleased]

### Добавлено (фокус-режим карточки отчёта)

- Фокус-режим карточки отчёта (Ctrl+Shift+F, кнопка 🧘 в шапке карточки) —
  пока открыта карточка одного отчёта, прячет шапку и боковую панель
  вкладок за самой карточкой, которая при этом разворачивается на всё
  окно вместо обычной модалки по центру. У этой карточки нет текста,
  который печатают (в отличие от zen mode редакторов) — отвлекает
  контекст вокруг: лента/список/доска, видные сквозь блюр модалки.
  Хоткей не пересекается ни с одним из существующих (см.
  shortcuts-help.js) и работает только пока карточка правда открыта.
  Настройки → «🧘 Фокус-режим при открытии отчёта» запоминает
  привычку — тот же приём, что у плотности таблиц/темы (localStorage)

### Добавлено (второе окно — открепить отчёт)

- «📌 Открепить в окне» в карточке отчёта — маленькое отдельное окно
  (Tauri 2 multiwebview, `@tauri-apps/api/webviewWindow`) с read-only
  статусом/сроком/чек-листом/последними заметками отчёта, поверх
  остальных окон по умолчанию (переключается кликом по 📌 в самом
  окне) — держать отчёт на виду, работая в другой программе, не
  переключаясь обратно в студию. Свой минимальный HTML-вход
  (`src/pin.html` → `app/pin-window.js`, отдельная запись в
  `vite.config.js`: rollupOptions.input), не `index.html` — тому
  нужен весь экран входа/вкладки/трей-обвязка, этому одна карточка.
  Окно единственное: открепление другого отчёта подменяет содержимое
  уже открытого, а не плодит второе (событие `pin-report-changed`
  через `@tauri-apps/api/event`). Токен — из того же нативного
  хранилища (`token_load`), что и в основном окне, обновление —
  раз в 15с и по фокусу окна. Права — отдельным узким файлом
  `src-tauri/capabilities/pin-window.json` (никаких диалогов/
  уведомлений/автозапуска/shell, только `core:default` +
  `core:window:allow-set-always-on-top`); главному окну добавлено
  только `core:webview:allow-create-webview-window` — именно этот
  идентификатор, а не `core:window:allow-create`, сверено по
  `node_modules/@tauri-apps/api/webviewWindow.js`
  (`plugin:webview|create_webview_window`), не по памяти о Tauri 1.
  Само появление и рендер второго нативного окна не проверить живьём
  в песочнице без GUI (Tauri IPC в обычном браузере — no-op) — логика
  создания окна и рендер карточки внутри него прогнаны отдельно в
  jsdom на собранном бандле (тот же приём, что у scripts/*-test.mjs)
  с замоканным `__TAURI_INTERNALS__.invoke`: создание окна с верным
  `label`/`url`, и сам pin-window.js на реальных полях ответа
  `/report/:id` (`status_label`, `assignees[].first_name`,
  `checklist.items[].done` и т.д. — тот же класс бага, что уже был
  с полем в ответе QC, здесь сверен построчно с report-detail.js)

### Исправлено

- `dsk_...`-токен десктоп-клиента (живёт месяцами) утекал в
  access-логи сервера при каждой аватарке/баннере/постере — `<img
  src>` не может послать заголовок `X-Init-Data`, поэтому auth уходил
  в query-строку прямо тем же токеном, которым подписаны остальные
  запросы. Теперь для картинок отдельный короткоживущий (5 минут)
  обменник — `mediaUrl()` в `src/app/api.js` берёт `mtok` через новую
  `POST /api/media/token` (сервер: `miniapp/server.py`), а не сам
  `dsk_...`. См. `SECURITY.md`

### Добавлено (нативные ffmpeg/OS-возможности)

- Волна с маркерами находок в QC звука: под результатом теперь рисуется
  огибающая амплитуды (min/max по бакетам, из бандлового ffmpeg —
  раньше он использовался только для декодирования под сам анализ),
  с подсветкой диапазонов находок цветом по серьёзности. Раньше
  находки были только текстом с тайм-кодами — видно "когда", не видно
  "как выглядит сигнал в этот момент", тот же приём, что маркеры в
  Adobe Audition/iZotope RX
- «✂ Фрагмент» у каждой находки QC — вырезает секунду до/после
  проблемного места и сохраняет отдельным WAV через нативный диалог
  сохранения, всё через тот же ffmpeg. Изолировать конкретный щелчок
  клиппинга или обрыв паузы для коллеги/режиссёра раньше значило
  пересылать исходник целиком
- «📂 Показать в папке» — кнопка-действие в тосте после скачивания
  файла отчёта или экспорта QC-фрагмента, открывает системный
  файловый менеджер на нужном каталоге (plugin:shell|open, те же
  права, что уже даёт «открыть в браузере» у карточки «Команда»)
- Прогресс пакетного QC (несколько файлов подряд) — на иконке в
  панели задач (ITaskbarList3 на Windows), не только полоской внутри
  окна: во время долгого прогона окно часто свёрнуто в трей, и
  единственный до сих пор индикатор был просто не виден
- Системное уведомление «QC завершён — N замечаний» по итогам
  пакетного прогона, если в этот момент окно не на экране — раньше
  результат был только тостом внутри окна, которое в такой момент
  никто не видит

### Добавлено (экспериментальная UX-сессия)

- Избранное в «Списке»: звёздочка у номера отчёта, чисто локальная
  метка (сервер о ней не знает — нет такого поля в API), хранится в
  localStorage на пользователя. Чип «★ Избранное» рядом с «Мои» и
  «Просрочено» показывает только помеченные отчёты
- Клавиша «?» — окно со списком горячих клавиш (иконка ⌨️ в шапке —
  то же самое мышью). Ctrl/Cmd+1…6 переключают вкладки по их позиции
  в сайдбаре — то, чего тач-интерфейс мини-аппа физически не может дать
- Правый клик по строке в «Списке»: открыть, избранное, статус,
  исполнитель, приоритет, срок, копия номера — весь набор быстрых
  действий без похода внутрь карточки, тем же контекстным меню, что
  уже было у строк «Команда» в профиле (см. utils.js showContextMenu)
- Ctrl+Enter в поле заметки на карточке отчёта отправляет её, не
  отвлекаясь на кнопку мышью — привычка из Slack/GitHub/Linear. Обычный
  Enter не трогали: заметки часто многострочные (несколько тайм-кодов
  правок подряд)
- Доска: колонки можно свернуть в узкую полоску (стрелка в заголовке
  колонки) — «Завершено»/«Отменено» обычно не нужны развёрнутыми, но
  раньше занимали на широкой доске столько же места, сколько активные
  статусы. Счётчик остаётся виден и свёрнутым. Состояние — локальное,
  на пользователя (localStorage), не серверное поле
- Командная палитра (Ctrl+K) с пустым запросом показывает недавно
  открытые отчёты сверху над действиями — тот же приём, что "Recent
  Files" в VS Code/Raycast. Как только начали печатать, это уже
  обычный поиск, MRU прячется
- Настройки → «Плотность таблиц»: обычная/компактная — десктопу есть
  смысл дать выбор (мышь наводится точно, мини-аппу с тач-целями
  такой режим только мешал бы)
- Последняя открытая вкладка переживает полный перезапуск приложения,
  не только вход/выход — студия держит окно открытым весь день, и
  открывать заново «Обзор» после каждого закрытия окна набегало
  десятками лишних кликов
- Диапазонное выделение Shift+клик по чекбоксу в «Списке» — привычка
  из проводника/почты. Отмечать полсотни строк подряд по одной раньше
  приходилось вручную
- Пустое состояние «Списка» — по конкретной причине под активный
  быстрый фильтр («Просроченных нет — всё по срокам», «Пока нет
  избранного — нажмите ☆…»), а не одна общая заглушка «Ничего не
  найдено» на все случаи

### Изменено (экспериментальная UX-сессия)

- Тосты различаются не только цветом полоски слева, но и иконкой
  (⚠️ ошибка, ✓ успех) — цветную полоску в 3px легко не заметить
  боковым зрением, пока смотришь на таблицу, а не на угол экрана
- Переключение светлой/тёмной темы — плавный crossfade через View
  Transitions API вместо мгновенного щелчка (WebView2 на Windows,
  куда собирается единственная реальная сборка, — Chromium 111+, API
  поддерживается; без поддержки — тот же мгновенный переход, что и
  раньше). Уважает prefers-reduced-motion

### Исправлено

- При запуске в трее появлялись две иконки — одна рабочая (клик
  сворачивает/разворачивает окно, есть меню), вторая ничего не делает.
  Причина — `app.trayIcon` в `tauri.conf.json` заставляет Tauri v2 сам
  создать иконку трея по умолчанию, а `main.rs` в `.setup()` строит свою,
  с меню и обработчиками кликов, — получались два трея на одну иконку в
  `bundle.icon`. Убрали декларативный `trayIcon` из конфига: код в
  `main.rs` уже сам берёт иконку через `app.default_window_icon()`, так
  что вторая иконка не нужна.

### Добавлено

- Заметки с тайм-кодом: у поля заметки появилось отдельное поле времени,
  а само время показывается меткой перед текстом. Когда заметок со
  временем больше одной, их можно переключить в порядок по дорожке —
  получается список правок, а не хронология написания
- Кнопка «QC дорожки» прямо в карточке отчёта: прогоняет локальный QC по
  выбранному файлу и одной кнопкой переносит все находки в заметки
  этого отчёта — каждую со своим временем начала
- `npm test` — общий прогон всех проверок на собранном бандле
  (verify + доска + профиль + заметки), он же теперь гоняется в CI

### Добавлено

- QC звука пакетом: несколько перетащенных (или выбранных в диалоге)
  дорожек считаются по очереди и показываются одной сводной таблицей —
  файл, пик, RMS, находки, — вместо стопки модалок друг поверх друга.
  Клик по строке раскрывает находки, кнопка «сначала проблемные»
  пересортировывает. Один файл по-прежнему открывает подробную карточку
- Вкладка «Я»: кольцо стажа объясняет себя — тир, дни в команде и
  сколько осталось до следующего порога (пороги 30/180/365 теперь один
  список и для свечения кольца, и для полоски)
- Вкладка «Я»: полученные награды показываются чипами, неполученные —
  строкой с остатком и полоской («50+ закрыто — 1 / 50»); раньше
  прогресс лежал в атрибуте title и был виден только под курсором
- Вкладка «Я»: пустые руки вместо «пока нечего показать» предлагают
  свободные серии (сколько их и сколько просрочено) и уводят в «Список»
  с фильтром «без исполнителя»
- Вкладка «Я»: у незаданной цели месяца показывается темп студии за
  месяц, и диалог открывается с этим числом
- `npm run test:profile` — проверка вкладки «Я» и пакетного QC на
  собранном бандле (19 проверок), гоняется в CI

### Изменено

- Рейтинг на профиле прячется, пока в таблице меньше двух человек:
  «#1 из 1» — не достижение, а следствие того, что серии закрывал один
- Прочерк в «Вовремя» объясняет себя: считается по отчётам со сроком

### Исправлено (ревью кода)

- Голосование за тайтл из детальной шторки падало с TypeError и молча
  ничего не делало (`castVote` искал предка `.vote-card`, которого там
  нет)
- Тяжёлые Rust-команды (QC звука, проверка и скачивание обновления)
  выполнялись в главном потоке и морозили окно; из-за этого же
  прогресс-бар обновления не двигался до самого конца скачивания
- Выход из аккаунта не чистил загруженные вкладки и точки отсчёта
  фоновых опросов: следующий вошедший на той же машине видел чужие
  отчёты и получал уведомление «вам назначено N новых отчётов» на весь
  свой список
- «Показать ещё» в Ленте и на Доске вешало второй обработчик на уже
  показанные элементы — клик по ним открывал карточку отчёта дважды
- Диалоги смены статуса/приоритета и выбор роли в dev-панели
  открывались на первом пункте списка вместо текущего значения —
  «Применить», нажатое не глядя, откатывало отчёт в черновик, а роль
  стирало в пустую
- Имя пользователя в шапке появлялось только сразу после ввода кода:
  `/whoami` его не возвращает, теперь оно приходит из `/me`
- Ответ 401/403 больше не сыплется тостами до перезапуска — клиент
  возвращает на экран входа
- Вкладка, упавшая по сети, больше не остаётся с текстом ошибки
  навсегда (пометка «загружено» ставится только после успеха)
- Ошибка на второй странице «Истории»/«Активности» больше не оставляет
  кнопку навсегда нажатой
- Гонка при быстром переключении сезонов в «Тайтлах» (ответ по старому
  сезону затирал новый)
- Ctrl+F больше не перехватывается поверх модалок и на других вкладках;
  Escape в диалоге обновления больше не подвешивает проверку навсегда
- Скачивание вложения отчёта: ссылка `target="_blank"` в webview не
  работала вовсе, а токен уезжал в query-строку — теперь файл тянет
  Rust с токеном в заголовке, путь выбирает нативный диалог
- Двойное экранирование имён на «Обзоре» (`esc()` внутри JSX поверх
  собственного экранирования Solid)
- Текст в интерфейсе снова можно выделить и скопировать
  (`user-select: none` стоял на всём `body`)
- ffmpeg больше не мигает консольным окном на Windows, а результат его
  поиска кэшируется на процесс
- Занятая другим приложением комбинация Ctrl+Shift+P больше не валит
  запуск клиента; трей не паникует при отсутствии иконки
- Переключение канала обновлений сохраняется сразу (`store.save()`), а
  альфа-канал наконец видит свои сборки (GithubSource игнорировал
  pre-release, которыми альфа и публикуется)
- `.aac` перестал быть «неподдерживаемым» при перетаскивании на
  карточку отчёта (списки расширений в QC и загрузке разъехались)

### Изменено

- `upload_report_file` читает с диска только те файлы, которые
  пользователь сам перетащил в окно (плюс лимит размера и проверка
  расширения на стороне Rust)
- Номер версии — один на весь проект: `VERSION` → `__APP_VERSION__`
  (Vite) вместо литерала в `settings.js`, который CI правил sed'ом
- `build.yml` и `build-alpha.yml` публикуют релизы в одной
  concurrency-группе (push с изменением VERSION запускает оба сразу)
- CI гоняет `npm run lint` и `npm run verify`; eslint/jsdom добавлены в
  devDependencies, без них ни то, ни другое не запускалось
- Прод-CSP без `ws://localhost:1420` (для Vite-HMR заведён отдельный
  `devCsp`), `img-src` сужен до своего API
- dependabot следит и за `package.json`
- SECURITY.md описывает реальное положение дел с подписью обновлений
  (её нет) и с токеном в URL картинок

- Фронтенд переведён на Vite (`src/` — исходники, `dist/` — сборка,
  больше не в git); вкладка «Обзор» переписана на SolidJS первым
  примером частичной миграции с vanilla JS — остальные вкладки пока
  без изменений
- Вкладка «Я» — широкая раскладка под окно ПК (карточка человека
  прилипает слева при скролле), ховер-карточка профиля при наведении
  на любой аватар в приложении, контекстное меню по правому клику в
  «Команде», системное уведомление о выполнении цели месяца
- Доска — колесо мыши и drag пустого места теперь скроллят её по
  горизонтали
- Альфа-релиз на GitHub помечается как Pre-release
- `cleanup_caches.yml` — ежедневная чистка просроченных кэшей
  `actions/cache` (их плодит `codeql.yml`)
