# legacy_python_client

Старый десктоп-клиент на Python + Tkinter — предшественник текущего
Tauri-приложения (см. [корневой README](../README.md)). Оставлен в
репозитории для истории миграции, не используется и не поддерживается:
новые фичи и фиксы идут только в `src-tauri/`/`dist/`.
